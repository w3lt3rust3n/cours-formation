//déclaration d'une variable: 
let monPrenom ;     
let dept;


//ou :

var monPrenom;
var dept;


// déclaration de la variable puis initialisation :
let monPrenom;
monPrenom = "anne";

// déclaration et initialisation en même temps :
 let dept = 76;


 
//on modifie la valeur stockée dans la variable dpt :

dpt=27 // la variable stocke à présent la valeur 27 l'ancienne valeur est écrasée.

// le = n'est pas l'opérateur mathématique, c'est un opérateur d'assignation (ou affectation).
